# Rust build output
/target

# Fetched pdfium runtime library (downloaded by `make pdfium`, not committed)
/vendor/

# Local sample / scratch files used for manual testing
/sample.*
/*.tmp

# macOS
.DS_Store
